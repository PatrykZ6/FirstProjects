#-*- coding: utf8 -*-

"""
Write a Python program to compute the distance between the points (x1, y1) and (x2, y2).

Napisz program w języku Python, który obliczy odległość między punktami (x1, y1) i (x2, y2).

"""

import math

p1 = [2,4]
p2 = [6,3]

def odcinek(x1,y1,x2,y2):
  odcinek_dlugosc = math.sqrt((x2-x1)**2+(y2-x1)**2)
  return odcinek_dlugosc

print(odcinek(p1[0],p1[1],p2[0],p2[1]))
